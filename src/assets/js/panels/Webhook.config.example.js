/**
 * Copie ce fichier en "webhook.config.js" (même dossier) et remplace la
 * valeur ci-dessous par ta vraie URL de webhook Discord.
 *
 * webhook.config.js est dans .gitignore : il ne sera jamais commité sur
 * GitHub, contrairement à ce fichier .example.js qui reste public sans
 * problème puisqu'il ne contient aucune vraie URL.
 */
export const CRASH_REPORT_DISCORD_WEBHOOK = '';